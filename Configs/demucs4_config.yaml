dataset:
  train_data_path: "train.pt"
  val_data_path: "valid.pt"
  eval_data_path: "eval.pt"
  batch_size: 16
  num_workers: 4

model:
  name: "AudioSeparationModel"
  learning_rate: 0.001
  optimizer: "AdamW"
  scheduler: "CosineAnnealingWarmRestarts"
  max_epochs: 50
  precision: 16
  accumulate_grad_batches: 2
  T_0: 10
  T_mult: 1
  eta_min: 1e-6
  save_checkpoint_path: "checkpoints/audio_model"
